;(function () {
    'use strict';

    var module = angular.module('mtg.variables', []);

    module.value('zIndex', 40);
    module.value('currentCard',0);
    module.value('playgroundHeight', 780);
    module.value('countMessage',0);

}());
